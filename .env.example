# Demo path (default offline — no network for bundles / voice beat)
NEXT_PUBLIC_DEMO_MODE=offline

# Server-side only — never NEXT_PUBLIC_
ELEVENLABS_API_KEY=
OPENAI_API_KEY=

# Optional override (default gpt-5.6-luna)
# OPENAI_MODEL=gpt-5.6-luna
